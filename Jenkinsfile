pipeline {
  environment {
    registry = "braverabbit/epam-exam"
   
  }
  agent { label 'jenkins_sl' } 
   stages {
    stage ('Py_tests') {
     steps {
      sh """
        pwd
      """
       }
      }
    
	}
	}
